# Core runtime requirements
numpy>=1.23.5,<1.26
sentencepiece>=0.1.95,<0.2
tqdm>=4.64.0
pyyaml>=6.0,<7
typing-extensions>=4.4.0

# ML tooling
torch>=2.0.0,<2.2              # install via official PyTorch wheel for your CUDA/TPU
transformers>=4.30.0,<5.0
datasets==2.11.0
accelerate>=0.21.0
peft>=0.4.0

# Optional / recommended
wandb==0.15.0                  # safe baseline for logging (works with Python 3.10 + Torch 2.0)
webdataset==0.2.107            # streaming large datasets
deepspeed==0.10.0              # distributed training, memory efficiency
bitsandbytes==0.49.0           # latest stable, supports CUDA 11.8+/Torch 2.0
loguru>=0.7.0                  # nicer logging

# Dev / utility
pytest>=7.0.0
black>=23.3.0
ruff>=0.6.0